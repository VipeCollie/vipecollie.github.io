---
layout: default
title: "Codage : Classe"
date: 2025-11-08
categories: [blog, jekyll]
---

<style>
/*  */
.highlight {
  background: #f4f4f4;
  border-radius: 8px;
  padding: 1rem;
  overflow-x: auto;
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
  font-size: 0.95em;
}
</style>

# Codage : Classe chat



Voici les lignes de code que j'ai réalisées.



{% highlight python %}
class Animal:
    def __init__(self,nom,age):
        self.nom=nom
        self.age=age
        self.faim=1
        self.soif=2

    def se_nourrir(self):
        while self.faim>0:
            if self.faim>0:
                print(f"{self.nom} a faim")
                self.faim-=1
                print(f"{self.nom} a mangé")
                print(self.faim)
                print(f"{self.faim}")
            else:
                print(f"{self.Nom}n'a pas faim")

    def s_abreuver(self):
        while self.soif>0:
            if self.soif>0:
                print(f"{self.nom} a soif")
                self.soif-=1
                print(f"{self.nom} a bu")
                print(f"{self.soif}")
            else:
                print(f"{self.Nom}n'a pas soif")

    def veillir(self):
        self.age+=1
        print(f"{self.nom} a veilli")
        print(f"{self.nom} a maitenant {self.age} ans")



{% endhighlight %}
